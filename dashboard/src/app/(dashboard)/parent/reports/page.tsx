import { ParentReportsView } from '@/components/ui/parent/ParentReportsView';

export const metadata = {
  title: 'Academic Reports | Parent Dashboard',
};

export default function ParentReportsPage() {
  return <ParentReportsView />;
}
